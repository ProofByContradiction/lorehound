# Secrets — never commit these
.env
*.env
service_account*.json
credentials*.json
google-credentials*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local rules cache / downloads
cache/
data/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local agent state / worktrees (Claude Code) — never publish these …
.claude/*
# … except committed project skills (playbooks the team + agents share).
!.claude/skills/

# Experimental art variants — keep the chosen icon/banner, skip the bulk
assets/alternates/

# Table-extraction audit output (dev tool: scripts/table_audit.py)
table-audit/

# Bot runtime (managed by the `lorehound` shell function)
bot.log
.bot.pid
reextract.log
