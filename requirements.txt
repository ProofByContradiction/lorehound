# Discord
discord.py>=2.4,<3

# Google Drive (service-account auth + file download/export)
google-api-python-client>=2.100
google-auth>=2.20
google-auth-httplib2>=0.2

# PDF text extraction — PyMuPDF + pymupdf4llm (Markdown with headings + multi-
# column reading order; drives the heading-aware chunking). We pin pymupdf4llm
# 0.3.4 — the last release before 1.27.x made pymupdf-layout a HARD dependency.
# Its heading detection (font-size histogram) + column_boxes reading order are
# pure-Python and produce output identical to 1.27.x's ML-free path, but it
# leaves pymupdf-layout (-> onnxruntime + numpy, ~100MB) as an opt-in extra we
# don't install. Keep pymupdf modern (column_boxes needs >= 1.24.2).
pymupdf==1.27.2.3
pymupdf4llm==0.3.4

# Local .env loading (no-op in production if absent)
python-dotenv>=1.0

# NOTE: the search engine itself is a dependency-free BM25 (search_index.py),
# and with the 0.3.4 pin above there's no onnxruntime/numpy in the tree.
